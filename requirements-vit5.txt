# requirements-vit5.txt
# =====================
# Additional dependencies for the Vietnamese ViT5 two-stage pipeline.
# Mirrors Leaf/Tangsang architecture in Vietnamese.
#
# Install AFTER base requirements.txt:
#   python -m venv venv_vit5
#   .\venv_vit5\Scripts\activate
#   pip install -r requirements-vit5.txt

# Core ML (ViT5 requires newer transformers)
torch>=2.0.0
transformers>=4.42.0
tokenizers>=0.15.0
sentencepiece>=0.1.99
accelerate>=0.30.0

# LoRA fine-tuning (Giai đoạn 4-5)
peft>=0.11.0

# Dataset loading from HuggingFace Hub (Giai đoạn 3)
datasets>=2.19.0

# Vietnamese NLP
underthesea>=6.8.0        # Vietnamese sentence tokenization

# Language detection
langdetect>=1.0.9

# PDF extraction (for /generate/pdf endpoint)
pdfplumber>=0.10.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0
